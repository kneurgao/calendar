export interface EventElement {
  title: string;
  time: string;
  elevation: number;
  style: {
    width: number;
    height: number;
    marginTop: number;
  };
  textStyle: {
    fontSize: number;
    fontWeight: number;
    lineHeight: number;
  };
}

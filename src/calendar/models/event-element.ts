export interface EventElement {
  title: string;
  time: string;
  style: {
    width: string;
    height: number;
    marginTop: string;
  };
  textStyle: {
    fontSize: number;
    fontWeight: number;
    lineHeight: number;
  };
}
